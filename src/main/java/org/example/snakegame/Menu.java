package org.example.snakegame;

import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.layout.Pane;

public class Menu extends Scene {
    Pane root = new Pane();

    private Menu(Parent parent) {
        super(parent);
    }
}
